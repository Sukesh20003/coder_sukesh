// take the input form the user and print reverse ways
#include<stdio.h>
#include<string.h>
int main() {
    char str[40];
    puts("enter a string");
    gets(str);
    puts("the reverse is : ");
    puts(str);      // puts give auto \n (nextline) 
    return 0;
}