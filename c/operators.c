// addition of two number 
#include<stdio.h>
int main() {
    int a,b;
    printf("enter your number a:- ");
    scanf("%d", &a);  // taking input form the user , the value of a;
     printf("enter your number b:- ");
    scanf("%d", &b);
    int c = a+b;  
    printf("the sum of two number is :- %d", c);
    return 0;
}
   // same for finding the  multiply / subtraction / division ;