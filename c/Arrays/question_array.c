/*
Given an array of marks of 10  student(you can also take input from the user), 
if marks of any student is less than 35 print it 
roll number . [ roll number is your index number ].
*/
#include<stdio.h>
int main() {
    int arr[10];
    
    for(int i =0; i<=10;i++) {
        printf("enter your number :- %d", i+1);              // input 
        scanf("%d", &arr[i]);
    }

    int marks[i];
    for(int = 0; i<=10, i++) {
      // condition
    if(marks[i]< 35) {j
        printf("%d", i);                                   // i = index
    }
    
    return 0;
}
}
//................................................................................................................


